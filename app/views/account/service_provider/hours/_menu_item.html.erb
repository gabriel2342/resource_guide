<% if can? :read, ServiceProvider::Hour.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :service_provider_hours]),
    label: t('service_provider/hours.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('service_provider/hours.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
